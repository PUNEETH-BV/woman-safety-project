services:
  - type: web
    name: safeguard-server
    runtime: node
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
